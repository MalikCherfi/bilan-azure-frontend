apiVersion: v1
kind: Service
metadata:
  name: front-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app.kubernetes.io/name: front
    app.kubernetes.io/instance: front-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP